<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.goodnightapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".graph" />
        <activity android:name=".listClicked" />
        <activity android:name=".tips" />

        <service
            android:name=".music"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".music" />
        <activity android:name=".Lentern" />
        <activity android:name=".recommend2" />
        <activity android:name=".Clockview1" />
        <activity android:name=".recommend" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Clockview"
            android:label="Clockview" />
    </application>

</manifest>